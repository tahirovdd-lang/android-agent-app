name: Build CaseonePOS APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout CaseonePOS Flutter source
        uses: actions/checkout@v4
        with:
          ref: flutter-source-import

      - name: Restore verified source
        id: restore
        shell: bash
        run: |
          set -euo pipefail
          SRC="$RUNNER_TEMP/caseonepos"
          rm -rf "$SRC" && mkdir -p "$SRC"
          cat \
            flutter_payload_v5/part_00 \
            flutter_payload_v5/part_01 \
            flutter_payload_v5/part_02_03 \
            flutter_payload_v5/part_04_05 \
            flutter_payload_v5_fix/p06_0 \
            flutter_payload_v5_fix/p06_1 \
            flutter_payload_v5_fix/p07_0 \
            flutter_payload_v5_fix/p07_1 \
            flutter_payload_v5/part_08_09 \
            flutter_payload_v5/part_10_11 \
            flutter_payload_v5_fix/p12_0 \
            flutter_payload_v5_fix/p12_1 \
            flutter_payload_v5_fix/p13_0 \
            flutter_payload_v5_fix/p13_1 \
            flutter_payload_v5/part_14_15 \
            flutter_payload_v5/part_16_17 \
            flutter_payload_v5/part_18 \
            | tr -d '\r\n ' > "$RUNNER_TEMP/core_only.b64"
          test "$(wc -c < "$RUNNER_TEMP/core_only.b64")" -eq 186104
          base64 -d "$RUNNER_TEMP/core_only.b64" > "$RUNNER_TEMP/core_only.tar.xz"
          echo 'd9cbd6fe265b51bc8d3ecd3266233fcf92c57e283fcc03fdb732c8ba693fe47b  '"$RUNNER_TEMP/core_only.tar.xz" | sha256sum -c -
          xz -t "$RUNNER_TEMP/core_only.tar.xz"
          xz -dc "$RUNNER_TEMP/core_only.tar.xz" | tar -xf - -C "$SRC"
          test -s "$SRC/pubspec.yaml"
          test -s "$SRC/lib/main.dart"
          echo "CASEONE_SRC=$SRC" >> "$GITHUB_ENV"

      - name: Setup Flutter
        id: flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android project
        id: wrapper
        shell: bash
        run: |
          set -euo pipefail
          cd "$CASEONE_SRC"
          flutter create --platforms=android --org uz.caseonepos --project-name caseonepos .

      - name: Apply CaseonePOS branding
        id: branding
        shell: bash
        run: |
          set -euo pipefail
          cd "$CASEONE_SRC"
          mkdir -p assets/images assets/fonts
          LOGO="$GITHUB_WORKSPACE/app/src/main/res/drawable/caseonepos_logo.png"
          test -s "$LOGO"
          cp "$LOGO" assets/images/logo.png
          cp "$LOGO" assets/images/logo_icon.png

          MANIFEST=android/app/src/main/AndroidManifest.xml
          sed -i 's/android:label="[^"]*"/android:label="CaseonePOS"/' "$MANIFEST"
          rm -f android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
          for dir in android/app/src/main/res/mipmap-*dpi; do
            [ -d "$dir" ] || continue
            cp "$LOGO" "$dir/ic_launcher.png"
            cp "$LOGO" "$dir/ic_launcher_round.png"
          done
          mkdir -p android/app/src/main/res/drawable
          cp "$LOGO" android/app/src/main/res/drawable/caseonepos_logo.png
          sed -i 's#android:icon="@[^"]*"#android:icon="@mipmap/ic_launcher"#' "$MANIFEST"
          if grep -q 'android:roundIcon=' "$MANIFEST"; then
            sed -i 's#android:roundIcon="@[^"]*"#android:roundIcon="@mipmap/ic_launcher_round"#' "$MANIFEST"
          else
            sed -i 's#<application#<application android:roundIcon="@mipmap/ic_launcher_round"#' "$MANIFEST"
          fi

          cat > android/app/src/main/res/drawable/launch_background.xml <<'XML'
          <?xml version="1.0" encoding="utf-8"?>
          <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
              <item android:drawable="@android:color/white" />
              <item><bitmap android:gravity="center" android:src="@drawable/caseonepos_logo" /></item>
          </layer-list>
          XML

          mkdir -p android/app/src/main/res/values-v31
          cat > android/app/src/main/res/values-v31/styles.xml <<'XML'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
                  <item name="android:windowSplashScreenBackground">#FFFFFF</item>
                  <item name="android:windowSplashScreenAnimatedIcon">@drawable/caseonepos_logo</item>
                  <item name="android:windowSplashScreenIconBackgroundColor">#FFFFFF</item>
                  <item name="android:windowLightStatusBar">true</item>
                  <item name="android:windowNoTitle">true</item>
              </style>
              <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
                  <item name="android:windowBackground">?android:colorBackground</item>
              </style>
          </resources>
          XML

          APP_GRADLE=android/app/build.gradle.kts
          sed -i -E 's/compileSdk = flutter\.compileSdkVersion/compileSdk = 36/' "$APP_GRADLE"
          sed -i -E 's/compileSdk = [0-9]+/compileSdk = 36/' "$APP_GRADLE"
          grep -n 'compileSdk' "$APP_GRADLE"
          grep -n 'CaseonePOS\|icon\|roundIcon' "$MANIFEST"

      - name: Resolve dependencies
        id: deps
        shell: bash
        run: |
          set -euo pipefail
          cd "$CASEONE_SRC"
          flutter pub get
          PICKER="$(find "$PUB_CACHE/hosted" -path '*/file_picker-8.3.7/android/build.gradle' -print -quit)"
          test -n "$PICKER"
          sed -i -E 's/compileSdkVersion[[:space:]]+[0-9]+/compileSdkVersion 36/' "$PICKER"
          sed -i -E 's/compileSdk[[:space:]]*=[[:space:]]*[0-9]+/compileSdk = 36/' "$PICKER"
          sed -i -E 's/compileSdk[[:space:]]+[0-9]+/compileSdk 36/' "$PICKER"
          grep -n 'compileSdk' "$PICKER"
          grep -qE 'compileSdk(Version)?([[:space:]]*=)?[[:space:]]*36' "$PICKER"

      - name: Build CaseonePOS APK
        id: apk
        shell: bash
        run: |
          set -euo pipefail
          cd "$CASEONE_SRC"
          flutter build apk --debug
          APK=build/app/outputs/flutter-apk/app-debug.apk
          test -s "$APK"
          cp "$APK" "$GITHUB_WORKSPACE/CaseonePOS-1.1.1.apk"
          sha256sum "$GITHUB_WORKSPACE/CaseonePOS-1.1.1.apk" > "$GITHUB_WORKSPACE/CaseonePOS-1.1.1.sha256"
          ls -lh "$GITHUB_WORKSPACE/CaseonePOS-1.1.1.apk"

      - name: Upload CaseonePOS APK
        id: artifact
        uses: actions/upload-artifact@v4
        with:
          name: CaseonePOS-APK
          path: |
            CaseonePOS-1.1.1.apk
            CaseonePOS-1.1.1.sha256
          retention-days: 30
          if-no-files-found: error

      - name: Record build result
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +e
          cd "$GITHUB_WORKSPACE"
          {
            echo "CaseonePOS APK build report"
            echo "run_id=$GITHUB_RUN_ID"
            echo "job_status=${{ job.status }}"
            echo "restore=${{ steps.restore.outcome }}"
            echo "flutter=${{ steps.flutter.outcome }}"
            echo "wrapper=${{ steps.wrapper.outcome }}"
            echo "branding=${{ steps.branding.outcome }}"
            echo "dependencies=${{ steps.deps.outcome }}"
            echo "apk_build=${{ steps.apk.outcome }}"
            echo "artifact=${{ steps.artifact.outcome }}"
            if [ -s CaseonePOS-1.1.1.apk ]; then
              echo "apk_present=yes"
              echo "apk_size=$(wc -c < CaseonePOS-1.1.1.apk)"
              sha256sum CaseonePOS-1.1.1.apk
            else
              echo "apk_present=no"
            fi
          } > caseonepos_build_status.txt
          CONTENT=$(base64 -w0 caseonepos_build_status.txt)
          SHA=$(gh api "repos/$GITHUB_REPOSITORY/contents/caseonepos_build_status.txt?ref=main" --jq .sha 2>/dev/null || true)
          if [ -n "$SHA" ]; then
            gh api --method PUT "repos/$GITHUB_REPOSITORY/contents/caseonepos_build_status.txt" -f message='CI: update CaseonePOS APK build status' -f content="$CONTENT" -f branch=main -f sha="$SHA" >/dev/null || true
          else
            gh api --method PUT "repos/$GITHUB_REPOSITORY/contents/caseonepos_build_status.txt" -f message='CI: add CaseonePOS APK build status' -f content="$CONTENT" -f branch=main >/dev/null || true
          fi
