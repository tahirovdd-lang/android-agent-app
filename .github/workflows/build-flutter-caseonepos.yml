name: Validate CaseonePOS Flutter payload

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validation is temporarily manual during source upload
        run: echo "Flutter source upload in progress"
